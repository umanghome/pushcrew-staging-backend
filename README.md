# pushcrew-staging-backend
Backend for PushCrew Staging's Slack commands

#### Instructions:
* Insert the correct credentials in ```index.js```
* Deploy on Heroku, or wherever you want. ```node index.js```